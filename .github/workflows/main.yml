name: YAFC Club TV Automation

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  heartbeat:
    runs-on: ubuntu-latest

    steps:
      - name: Club TV Heartbeat
        run: |
          echo "YAFC Club TV is alive"
          date
